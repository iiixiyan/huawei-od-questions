{"title": "预测新能源发电量(100分)", "topics": ["模拟"], "pid": "P14404", "difficulty": 3, "date": "2026年6月22日", "description_md": "# 题目内容\n\n某地面光伏电站按区域划分为多个子阵。每个子阵的发电能力有明确的约束：\n- 发电能力上限（$max\\_capacity$）\n- 发电能力下限（$min\\_capacity$）\n- 基准发电量（$base\\_generation$）\n\n系统需要根据这些约束，预测整个电站的发电量。预测策略是：\n1. 每个子阵有一个基准发电量\n2. 如果总预测发电量超过电站总容量，需要按比例缩减\n3. 如果总预测发电量低于总容量下限，需要按比例放大\n4. 最终预测发电量向上取整为整数\n\n## 调整策略\n### 缩减场景\n当总发电量超过电站容量时，按各子阵的可用缩减空间比例分配缩减量\n\n可用缩减空间 $=$ $base\\_generation$ $-$ $min\\_capacity$；表示该子阵还能减少多少发电量而不低于下限\n\n缩减量按此比例分配：$缩减量_i$ = 总缩减量 × ($可用缩减空间_i$ / 总可用缩减空间)\n\n### 放大场景\n\n当总发电量低于总下限时，按各子阵的可用放大空间比例分配增加量\n\n可用放大空间 = $max\\_capacity$ - $base\\_generation$；表示该子阵还能增加多少发电量而不超过上限\n\n增加量按此比例分配：$增加量_i$ = 总增加量 × ($可用放大空间_i$ / 总可用放大空间)\n\n## 补充说明\n### 输入\n\n$sub\\_arrays$: 子阵列表，每个子阵是一个三元组[$max\\_capacity$, $min\\_capacity$, \n$base\\_generation$]，均为整数\n\n$station\\_capacity$: 电站总容量上限（整数）\n\n### 输出\n\n\n返回一维数组，每个元素是对应子阵的预测发电量（向上整数）\n如果无法在约束范围内满足总容量要求，返回全 $0$ 数组\n\n### 特殊情况处理\n1. 如果所有子阵都达到下限，但仍超过电站容量：返回全 $0$ 数组\n2. 如果所有子阵都达到上限，但仍低于总下限：返回全 $0$ 数组\n\n### 要求\n最终预测发电量必须满足：$min\\_capacity \\le predicted\\_generation \\le max\\_capacity$\n1. 如果总预测发电量 $>$ $station\\_capacity$，需要按比例缩减\n2. 如果总预测发电量 $<$ 总下限（各子阵$min\\_capacity$之和），需要按比例放大\n3. 最终结果必须向上取整为整数（如使用$math.ceil$）\n\n\n\n## 样例1 \n\n\n\n**输入** \n\n\n\n```\n[[1000, 800, 900],[1200, 900, 1100],[800, 600, 700]],2800\n```\n\n\n\n**输出** \n\n\n\n```\n[900,1100,700]\n```\n\n\n\n\n\n## 样例2\n\n\n\n**输入** \n\n\n\n```\n[[1000, 800, 800],[1200, 900, 900],[800, 600, 600]],2600\n```\n\n\n\n**输出** \n\n\n\n```\n[800,900,600]\n```\n\n\n", "has_signature": false, "python_signature": null, "accept_rate": "25/451", "code_templates": {"py": "# 空白解题模板\n\nclass Solution:\n    def solve(self):\n        # Write your code here\n        pass\n", "java": "// 空白解题模板\n\nclass Solution {\n    public void solve() {\n        // Write your code here\n        \n    }\n}\n", "cc": "// 空白解题模板\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    void solve() {\n        // Write your code here\n        \n    }\n};\n", "js": "// JavaScript空白模板\n\n// Write your code here\n", "c": "// C语言空白模板\n\n#include <stdio.h>\n#include <stdlib.h>\n\n// Write your code here\n"}, "solution": "## 解题思路\n\n本题按子阵基准发电量做**一次**全局比例调整，再对结果**向上取整**。\n\n1. 读取每个子阵的 `max_capacity`、`min_capacity`、`base_generation`，记基准数组 `base`，并计算 `total = sum(base)`、`min_total = sum(min)`。\n2. **缩减**：若 `total > station_capacity`，总缩减量为 `total - station_capacity`。子阵 $i$ 的可缩减空间为 `base[i] - min[i]`，按空间占比分配缩减量；若可缩减空间之和为 $0$，说明全部已到下限仍超标，返回全 $0$。\n3. **放大**：若 `total < min_total`，总增加量为 `min_total - total`。子阵 $i$ 的可放大空间为 `max[i] - base[i]`，按空间占比分配增加量；若可放大空间之和为 $0$，返回全 $0$。\n4. 对调整后的实数值逐元素 `ceil` 得到整数答案。\n\n核心观察：只需判断「超标」或「低于总下限」两种情形之一，按比例一次性分配即可；注意浮点误差与向上取整。\n\n## 复杂度分析\n\n- 时间复杂度：$O(n)$，其中 $n$ 为子阵个数；仅需两次线性扫描与一次分配。\n- 空间复杂度：$O(n)$，存放基准、上下界与中间实数结果。\n\n## 代码实现\n\n#code-switcher\n```python\nimport math\nfrom typing import List\n\n\nclass Solution:\n    def predictGeneration(\n        self, sub_arrays: List[List[int]], station_capacity: int\n    ) -> List[int]:\n        n = len(sub_arrays)\n        if n == 0:\n            return []\n\n        base = [row[2] for row in sub_arrays]\n        mins = [row[1] for row in sub_arrays]\n        maxs = [row[0] for row in sub_arrays]\n\n        total = sum(base)\n        min_total = sum(mins)\n        values = [float(x) for x in base]\n\n        # 总基准超过电站容量：按可缩减空间比例下调\n        if total > station_capacity:\n            need = total - station_capacity\n            spaces = [base[i] - mins[i] for i in range(n)]\n            space_sum = sum(spaces)\n            if space_sum == 0:\n                return [0] * n\n            for i in range(n):\n                values[i] = base[i] - need * spaces[i] / space_sum\n        # 总基准低于各子阵下限之和：按可放大空间比例上调\n        elif total < min_total:\n            need = min_total - total\n            spaces = [maxs[i] - base[i] for i in range(n)]\n            space_sum = sum(spaces)\n            if space_sum == 0:\n                return [0] * n\n            for i in range(n):\n                values[i] = base[i] + need * spaces[i] / space_sum\n\n        # 向上取整；减微小量避免浮点边界误判\n        return [math.ceil(v - 1e-12) for v in values]\n```\n\n```java\npublic class Solution {\n    public int[] predictGeneration(int[][] sub_arrays, int station_capacity) {\n        int n = sub_arrays.length;\n        if (n == 0) return new int[0];\n\n        int[] base = new int[n];\n        int[] mins = new int[n];\n        int[] maxs = new int[n];\n        for (int i = 0; i < n; i++) {\n            maxs[i] = sub_arrays[i][0];\n            mins[i] = sub_arrays[i][1];\n            base[i] = sub_arrays[i][2];\n        }\n\n        long total = 0, minTotal = 0;\n        for (int i = 0; i < n; i++) {\n            total += base[i];\n            minTotal += mins[i];\n        }\n\n        double[] values = new double[n];\n        for (int i = 0; i < n; i++) values[i] = base[i];\n\n        if (total > station_capacity) {\n            long need = total - station_capacity;\n            long spaceSum = 0;\n            for (int i = 0; i < n; i++) spaceSum += base[i] - mins[i];\n            // 全部已到下限仍超标，无法分配\n            if (spaceSum == 0) return new int[n];\n            for (int i = 0; i < n; i++) {\n                double space = base[i] - mins[i];\n                values[i] = base[i] - need * space / (double) spaceSum;\n            }\n        } else if (total < minTotal) {\n            long need = minTotal - total;\n            long spaceSum = 0;\n            for (int i = 0; i < n; i++) spaceSum += maxs[i] - base[i];\n            if (spaceSum == 0) return new int[n];\n            for (int i = 0; i < n; i++) {\n                double space = maxs[i] - base[i];\n                values[i] = base[i] + need * space / (double) spaceSum;\n            }\n        }\n\n        int[] ans = new int[n];\n        for (int i = 0; i < n; i++) {\n            ans[i] = (int) Math.ceil(values[i] - 1e-12);\n        }\n        return ans;\n    }\n}\n```\n\n```cpp\n#include <cmath>\n#include <vector>\n\nusing namespace std;\n\nclass Solution {\n public:\n  vector<int> predictGeneration(vector<vector<int>>& sub_arrays,\n                                int station_capacity) {\n    int n = (int)sub_arrays.size();\n    if (n == 0) return {};\n\n    vector<int> base(n), mins(n), maxs(n);\n    for (int i = 0; i < n; i++) {\n      maxs[i] = sub_arrays[i][0];\n      mins[i] = sub_arrays[i][1];\n      base[i] = sub_arrays[i][2];\n    }\n\n    long long total = 0, min_total = 0;\n    for (int i = 0; i < n; i++) {\n      total += base[i];\n      min_total += mins[i];\n    }\n\n    vector<double> values(n);\n    for (int i = 0; i < n; i++) values[i] = base[i];\n\n    if (total > station_capacity) {\n      long long need = total - station_capacity;\n      long long space_sum = 0;\n      for (int i = 0; i < n; i++) space_sum += base[i] - mins[i];\n      if (space_sum == 0) return vector<int>(n, 0);\n      for (int i = 0; i < n; i++) {\n        double space = base[i] - mins[i];\n        values[i] = base[i] - need * space / (double)space_sum;\n      }\n    } else if (total < min_total) {\n      long long need = min_total - total;\n      long long space_sum = 0;\n      for (int i = 0; i < n; i++) space_sum += maxs[i] - base[i];\n      if (space_sum == 0) return vector<int>(n, 0);\n      for (int i = 0; i < n; i++) {\n        double space = maxs[i] - base[i];\n        values[i] = base[i] + need * space / (double)space_sum;\n      }\n    }\n\n    vector<int> ans(n);\n    for (int i = 0; i < n; i++) {\n      ans[i] = (int)ceil(values[i] - 1e-12);\n    }\n    return ans;\n  }\n};\n```\n\n```javascript\nvar predictGeneration = function(sub_arrays, station_capacity) {\n    var n = sub_arrays.length;\n    if (n === 0) return [];\n\n    var base = [], mins = [], maxs = [];\n    var total = 0, minTotal = 0;\n    for (var i = 0; i < n; i++) {\n        maxs[i] = sub_arrays[i][0];\n        mins[i] = sub_arrays[i][1];\n        base[i] = sub_arrays[i][2];\n        total += base[i];\n        minTotal += mins[i];\n    }\n\n    var values = base.slice();\n\n    // 总基准超过电站容量：按可缩减空间比例下调\n    if (total > station_capacity) {\n        var need = total - station_capacity;\n        var spaceSum = 0;\n        for (var i = 0; i < n; i++) spaceSum += base[i] - mins[i];\n        if (spaceSum === 0) return new Array(n).fill(0);\n        for (var i = 0; i < n; i++) {\n            var space = base[i] - mins[i];\n            values[i] = base[i] - need * space / spaceSum;\n        }\n    }\n    // 总基准低于各子阵下限之和：按可放大空间比例上调\n    else if (total < minTotal) {\n        var need = minTotal - total;\n        var spaceSum = 0;\n        for (var i = 0; i < n; i++) spaceSum += maxs[i] - base[i];\n        if (spaceSum === 0) return new Array(n).fill(0);\n        for (var i = 0; i < n; i++) {\n            var space = maxs[i] - base[i];\n            values[i] = base[i] + need * space / spaceSum;\n        }\n    }\n\n    // 向上取整，减去微小量避免浮点边界\n    var result = [];\n    for (var i = 0; i < n; i++) {\n        result[i] = Math.ceil(values[i] - 1e-12);\n    }\n    return result;\n};\n```\n\n```c\n#include <stdlib.h>\n#include <math.h>\n\nint* predictGeneration(int** sub_arrays, int subArraysSize, int station_capacity, int* returnSize) {\n    int n = subArraysSize;\n    *returnSize = n;\n    if (n == 0) return (int*)malloc(0);\n\n    int* base = (int*)malloc(n * sizeof(int));\n    int* mins = (int*)malloc(n * sizeof(int));\n    int* maxs = (int*)malloc(n * sizeof(int));\n\n    long long total = 0, min_total = 0;\n    for (int i = 0; i < n; i++) {\n        maxs[i] = sub_arrays[i][0];\n        mins[i] = sub_arrays[i][1];\n        base[i] = sub_arrays[i][2];\n        total += base[i];\n        min_total += mins[i];\n    }\n\n    double* values = (double*)malloc(n * sizeof(double));\n    for (int i = 0; i < n; i++) values[i] = (double)base[i];\n\n    // 总基准超过电站容量：按可缩减空间比例下调\n    if (total > station_capacity) {\n        long long need = total - station_capacity;\n        long long space_sum = 0;\n        for (int i = 0; i < n; i++) space_sum += base[i] - mins[i];\n        if (space_sum == 0) {\n            int* result = (int*)calloc(n, sizeof(int));\n            free(base); free(mins); free(maxs); free(values);\n            return result;\n        }\n        for (int i = 0; i < n; i++) {\n            double space = base[i] - mins[i];\n            values[i] = base[i] - need * space / (double)space_sum;\n        }\n    }\n    // 总基准低于各子阵下限之和：按可放大空间比例上调\n    else if (total < min_total) {\n        long long need = min_total - total;\n        long long space_sum = 0;\n        for (int i = 0; i < n; i++) space_sum += maxs[i] - base[i];\n        if (space_sum == 0) {\n            int* result = (int*)calloc(n, sizeof(int));\n            free(base); free(mins); free(maxs); free(values);\n            return result;\n        }\n        for (int i = 0; i < n; i++) {\n            double space = maxs[i] - base[i];\n            values[i] = base[i] + need * space / (double)space_sum;\n        }\n    }\n\n    // 向上取整，减去微小量避免浮点边界误判\n    int* result = (int*)malloc(n * sizeof(int));\n    for (int i = 0; i < n; i++) {\n        result[i] = (int)ceil(values[i] - 1e-12);\n    }\n\n    free(base); free(mins); free(maxs); free(values);\n    return result;\n}\n```\n\n#code-switcher\n\n#hot100-card {\n  title: \"LeetCode Hot 100系统复习笔记\",\n  btn: \"开始复习\",\n  href: \"/codenote/hot100\",\n}\n\n"}