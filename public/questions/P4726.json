{"title": "勇攀数字高峰(200分)", "topics": ["DFS"], "pid": "P4726", "difficulty": 6, "date": "2026年4月1日", "description_md": "## 题目描述\n\n你在给定的数字地形图中寻找登山路径，数字代表当前位置的海拔高度，要求从最低海拔出发，不断攀登，最终到达最高山峰。你需要寻找所有满足条件的登山路径。\n地图已经保证最低海拔和最高山峰都只有一个。\n\n## 路径条件\n\n* **登山规则**：路径上的海拔必须严格递增\n* **移动限制**：可以向上下左右 $4$ 个方向移动\n* **路径限制**：路径必须从最低海拔开始，到最高海拔结束\n* **访问控制**：每个地点只能走一次\n* **高度差限制**：每一步的攀登高度差必须大于 $0$，小于等于指定值\n\n## 输入格式\n\n输入一个二维数组表示的海拔图，维度为 $n \\times m$（$2 \\le n,m \\le 10$），每个元素都是一个整数 $x$\n输入一个整数参数表示单步最大允许的高度差\n\n## 输出格式\n\n输出满足条件的登山路径的数量\n\n## 示例1\n\n输入\n```\n[[1,2],[3,5]],2\n```\n输出\n```\n1\n```\n说明\n\n起点：最低点坐标 $(0,0)$，海拔高度 $1$\n终点：最高点坐标 $(1,1)$，海拔高度 $5$\n单步最大高度差：$2$\n可行路径：\n路径 $1$：$(0,0), (1,0), (1,1)$\n\n## 示例2\n\n输入\n```\n[[4,3],[3,2]],1\n```\n输出\n```\n2\n```\n说明\n\n起点：最低点坐标 $(1,1)$，海拔高度 $2$\n终点：最高点坐标 $(0,0)$，海拔高度 $4$\n单步最大高度差：$1$\n可行路径：\n路径 $1$：$(1,1), (0,1), (0,0)$\n路径 $2$：$(1,1), (1,0), (0,0)$\n\n## 示例3\n\n输入\n```\n[[1,3],[3,4]],1\n```\n输出\n```\n0\n```\n说明\n\n起点：最低点坐标 $(0,0)$，海拔高度 $1$\n终点：最高点坐标 $(1,1)$，海拔高度 $4$\n单步最大高度差：$1$\n可行路径：$0$\n", "has_signature": false, "python_signature": null, "accept_rate": "109/365", "code_templates": {"py": "# 空白解题模板\n\nclass Solution:\n    def solve(self):\n        # Write your code here\n        pass\n", "java": "// 空白解题模板\n\nclass Solution {\n    public void solve() {\n        // Write your code here\n        \n    }\n}\n", "cc": "// 空白解题模板\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    void solve() {\n        // Write your code here\n        \n    }\n};\n", "js": "// JavaScript空白模板\n\n// Write your code here\n", "c": "// C语言空白模板\n\n#include <stdio.h>\n#include <stdlib.h>\n\n// Write your code here\n"}, "solution": "## 解题思路\n\n本题在 $n \\times m$ 网格上统计从**唯一全局最小值**格子走到**唯一全局最大值**格子的路径条数。每一步只能走向四邻域中海拔**严格更高**的格子，且高度差不超过 `maxDiff`。路径上每个格子最多访问一次；由于海拔沿步严格递增，图中不存在环路，从任意格子沿合法边走向更高海拔构成有向无环图（DAG），因此可用 **记忆化搜索（DFS + memo）** 自终点（或自起点）统计路径数。\n\n具体做法：\n\n1. 扫描网格得到全局最小值、最大值及其坐标 $(s_x,s_y)$、$(t_x,t_y)$（题面保证各唯一）。\n2. 定义 `dfs(i,j)`：从 $(i,j)$ 出发，沿「海拔上升且步长 $\\in(0,\\text{maxDiff}]$」的四连通边走到终点的路径数。\n3. 边界：若 $(i,j)$ 为终点，返回 $1$；否则对四个方向尝试扩展，累加合法邻居的 `dfs` 结果。\n4. 用 `memo[i][j]`（或哈希表）记录已算状态，避免重复计算；严格递增保证无环，记忆化安全。\n\n常见假解：把问题当成最短路径只数层；忽略 `maxDiff` 上界；未固定全局最小为起点；在有多条汇入时不用记忆化导致超时（本题 $n,m\\le10$ 规模下暴力也可过，但记忆化是标准写法）。\n\n## 复杂度分析\n\n记 $V=nm \\le 100$。每个格子至多被 `dfs` 计算一次，每条有向边至多被松弛常数次，故时间复杂度 $O(nm)$，空间 $O(nm)$（网格本身与记忆化表同阶）。\n\n## 代码实现\n\n#code-switcher\n```python\nfrom functools import lru_cache\nfrom typing import List\n\n\nclass Solution:\n    def countHikingPaths(self, grid: List[List[int]], maxDiff: int) -> int:\n        n, m = len(grid), len(grid[0])\n        # 找出唯一最小、最大及其坐标\n        mn = min(grid[i][j] for i in range(n) for j in range(m))\n        mx = max(grid[i][j] for i in range(n) for j in range(m))\n        si = sj = ei = ej = -1\n        for i in range(n):\n            for j in range(m):\n                if grid[i][j] == mn:\n                    si, sj = i, j\n                if grid[i][j] == mx:\n                    ei, ej = i, j\n        dirs = ((1, 0), (-1, 0), (0, 1), (0, -1))\n\n        @lru_cache(maxsize=None)\n        def dfs(i: int, j: int) -> int:\n            if i == ei and j == ej:\n                return 1\n            h = grid[i][j]\n            tot = 0\n            for di, dj in dirs:\n                ni, nj = i + di, j + dj\n                if 0 <= ni < n and 0 <= nj < m:\n                    nh = grid[ni][nj]\n                    d = nh - h\n                    # 严格上升且单步高度差不超过 maxDiff\n                    if 0 < d <= maxDiff:\n                        tot += dfs(ni, nj)\n            return tot\n\n        return dfs(si, sj)\n```\n```java\nimport java.util.*;\n\npublic class Solution {\n    private int n, m, si, sj, ei, ej, maxDiff;\n    private int[][] grid;\n    private long[][] memo;\n    private boolean[][] done;\n\n    public long countHikingPaths(int[][] grid, int maxDiff) {\n        this.grid = grid;\n        this.maxDiff = maxDiff;\n        n = grid.length;\n        m = grid[0].length;\n        // 预处理全局最小/最大与起终点坐标\n        int mn = grid[0][0], mx = grid[0][0];\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < m; j++) {\n                mn = Math.min(mn, grid[i][j]);\n                mx = Math.max(mx, grid[i][j]);\n            }\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < m; j++) {\n                if (grid[i][j] == mn) {\n                    si = i;\n                    sj = j;\n                }\n                if (grid[i][j] == mx) {\n                    ei = i;\n                    ej = j;\n                }\n            }\n        memo = new long[n][m];\n        done = new boolean[n][m];\n        return dfs(si, sj);\n    }\n\n    private long dfs(int i, int j) {\n        if (i == ei && j == ej) return 1;\n        if (done[i][j]) return memo[i][j];\n        done[i][j] = true;\n        long tot = 0;\n        int h = grid[i][j];\n        int[][] dirs = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};\n        for (int[] d : dirs) {\n            int ni = i + d[0], nj = j + d[1];\n            if (ni < 0 || ni >= n || nj < 0 || nj >= m) continue;\n            int nh = grid[ni][nj];\n            int diff = nh - h;\n            // 仅统计严格递增且步长在 (0, maxDiff] 的边\n            if (diff > 0 && diff <= maxDiff) tot += dfs(ni, nj);\n        }\n        memo[i][j] = tot;\n        return tot;\n    }\n}\n```\n```cpp\n#include <algorithm>\n#include <functional>\n#include <vector>\n\nusing namespace std;\n\nclass Solution {\n   public:\n    long long countHikingPaths(vector<vector<int>>& grid, int maxDiff) {\n        int n = (int)grid.size(), m = (int)grid[0].size();\n        int mn = grid[0][0], mx = grid[0][0];\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < m; j++) {\n                mn = min(mn, grid[i][j]);\n                mx = max(mx, grid[i][j]);\n            }\n        int si = -1, sj = -1, ei = -1, ej = -1;\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < m; j++) {\n                if (grid[i][j] == mn) si = i, sj = j;\n                if (grid[i][j] == mx) ei = i, ej = j;\n            }\n        static long long memo[12][12];\n        static int vis[12][12];\n        static int stamp = 1;\n        ++stamp;\n        const int dirs[4][2] = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};\n        function<long long(int, int)> dfs = [&](int i, int j) -> long long {\n            if (i == ei && j == ej) return 1;\n            if (vis[i][j] == stamp) return memo[i][j];\n            vis[i][j] = stamp;\n            long long tot = 0;\n            int h = grid[i][j];\n            for (int k = 0; k < 4; k++) {\n                int ni = i + dirs[k][0], nj = j + dirs[k][1];\n                if (ni < 0 || ni >= n || nj < 0 || nj >= m) continue;\n                int nh = grid[ni][nj];\n                int d = nh - h;\n                if (d > 0 && d <= maxDiff) tot += dfs(ni, nj);\n            }\n            memo[i][j] = tot;\n            return tot;\n        };\n        return dfs(si, sj);\n    }\n};\n```\n```javascript\nvar countHikingPaths = function(grid, maxDiff) {\n    var n = grid.length, m = grid[0].length;\n    // 找出唯一最小、最大及其坐标\n    var mn = grid[0][0], mx = grid[0][0];\n    var si = 0, sj = 0, ei = 0, ej = 0;\n    for (var i = 0; i < n; i++) {\n        for (var j = 0; j < m; j++) {\n            var v = grid[i][j];\n            if (v < mn) { mn = v; si = i; sj = j; }\n            if (v > mx) { mx = v; ei = i; ej = j; }\n        }\n    }\n\n    // 记忆化数组，-1 表示未计算\n    var memo = new Array(n);\n    for (var i = 0; i < n; i++) {\n        memo[i] = new Array(m);\n        for (var j = 0; j < m; j++) memo[i][j] = -1;\n    }\n\n    function dfs(i, j) {\n        if (i === ei && j === ej) return 1;\n        if (memo[i][j] !== -1) return memo[i][j];\n        var tot = 0;\n        var h = grid[i][j];\n        // 四方向：上、下、左、右\n        if (i > 0) { var d = grid[i-1][j] - h; if (d > 0 && d <= maxDiff) tot += dfs(i-1, j); }\n        if (i + 1 < n) { var d = grid[i+1][j] - h; if (d > 0 && d <= maxDiff) tot += dfs(i+1, j); }\n        if (j > 0) { var d = grid[i][j-1] - h; if (d > 0 && d <= maxDiff) tot += dfs(i, j-1); }\n        if (j + 1 < m) { var d = grid[i][j+1] - h; if (d > 0 && d <= maxDiff) tot += dfs(i, j+1); }\n        memo[i][j] = tot;\n        return tot;\n    }\n\n    return dfs(si, sj);\n};\n```\n```c\nstatic long long memo[12][12];\nstatic int vis[12][12];\nstatic int stamp = 1;\nstatic int n_global, m_global, maxDiff_global;\nstatic int ei, ej;\nstatic int** g;\n\nstatic long long dfs(int i, int j) {\n    // 到达终点\n    if (i == ei && j == ej) return 1;\n    if (vis[i][j] == stamp) return memo[i][j];\n    vis[i][j] = stamp;\n    long long tot = 0;\n    int h = g[i][j];\n    int dirs[4][2] = {{1,0}, {-1,0}, {0,1}, {0,-1}};\n    for (int k = 0; k < 4; k++) {\n        int ni = i + dirs[k][0];\n        int nj = j + dirs[k][1];\n        if (ni < 0 || ni >= n_global || nj < 0 || nj >= m_global) continue;\n        int d = g[ni][nj] - h;\n        if (d > 0 && d <= maxDiff_global) tot += dfs(ni, nj);\n    }\n    memo[i][j] = tot;\n    return tot;\n}\n\nint countHikingPaths(int** grid, int gridSize, int* gridColSize, int maxDiff) {\n    int n = gridSize;\n    int m = gridColSize[0];\n    n_global = n;\n    m_global = m;\n    maxDiff_global = maxDiff;\n    g = grid;\n    stamp++;\n\n    // 找出唯一最小、最大及其坐标\n    int mn = grid[0][0], mx = grid[0][0];\n    int si = 0, sj = 0;\n    ei = 0, ej = 0;\n    for (int i = 0; i < n; i++) {\n        for (int j = 0; j < m; j++) {\n            int v = grid[i][j];\n            if (v < mn) { mn = v; si = i; sj = j; }\n            if (v > mx) { mx = v; ei = i; ej = j; }\n        }\n    }\n    return (int)dfs(si, sj);\n}\n```\n#code-switcher\n#hot100-card {\n  title: \"LeetCode Hot 100系统复习笔记\",\n  btn: \"开始复习\",\n  href: \"/codenote/hot100\",\n}"}