{"title": "奇偶三数之和(100分)", "topics": ["双指针"], "pid": "P14413", "difficulty": 4, "date": "2026年7月1日", "description_md": "# 题目内容\n\n给定一个包含 $n$ 个整数的数组 `nums` 和一个整数 `target`，请从数组中找出所有 **不重复** 的三元组 `[nums[i], nums[j], nums[k]]`，满足以下两个条件：\n\n1. **和为目标**：  \n   $nums[i] + nums[j] + nums[k] = target$\n\n2. **奇偶性约束**：  \n   三个元素中 **至少有两个** 是奇数。\n\n返回所有满足条件的不重复三元组。\n\n**提示：**\n\n- $3 \\leq nums.length \\leq 3000$\n- $-10^5 \\leq nums[i] \\leq 10^5$\n\n**补充说明**\n\n返回值输出格式：每个子数组内部的元素按数值升序排列，所有子数组之间按字典序升序排列（即先比较第一个元素，若相同再比较第二个，以此类推）。  \n示例：`[[-1,-1,2],[-1,0,1]]`\n\n# 输入描述\n\n输入为一个数组 `nums` 和一个目标整数 `target`。\n\n# 输出描述\n\n输出所有满足条件的不重复三元组。\n\n## 样例1\n\n**输入**\n\n```\n[-1,0,1,2,-1,-4],0\n```\n\n**输出**\n\n```\n[[-1,-1,2],[-1,0,1]]\n```\n\n**说明**\n\n无\n\n## 样例2\n\n**输入**\n\n```\n[1,2,3,4,5],9\n```\n\n**输出**\n\n```\n[[1,3,5]]\n```\n\n**说明**\n\n- `[1,3,5] = 9`，三个都是奇数，**通过**。  \n- `[2,3,4] = 9`，`3` 是奇数，但 `2` 和 `4` 都是偶数，只有一个奇数，**不通过**。\n\n## 样例3\n\n**输入**\n\n```\n[0,0,0],0\n```\n\n**输出**\n\n```\n[]\n```", "has_signature": false, "python_signature": null, "accept_rate": "34/135", "code_templates": {"py": "# 空白解题模板\n\nclass Solution:\n    def solve(self):\n        # Write your code here\n        pass\n", "java": "// 空白解题模板\n\nclass Solution {\n    public void solve() {\n        // Write your code here\n        \n    }\n}\n", "cc": "// 空白解题模板\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    void solve() {\n        // Write your code here\n        \n    }\n};\n", "js": "// JavaScript空白模板\n\n// Write your code here\n", "c": "// C语言空白模板\n\n#include <stdio.h>\n#include <stdlib.h>\n\n// Write your code here\n"}, "solution": "## 解题思路\n\n本题是经典「三数之和」的变体，额外增加了**奇偶性约束**：三元组中至少有两个元素为奇数。\n\n**算法核：排序 + 枚举 + 双指针**\n\n1. 首先将数组升序排序，便于跳过重复元素和控制双指针方向。\n2. 枚举第一个元素 `nums[i]`（`i` 从 `0` 到 `n-3`），若与前一元素相同则跳过以去重。\n3. 对固定的 `i`，使用双指针 `j = i+1`、`k = n-1` 在剩余部分中搜索：\n   - 若 `nums[i] + nums[j] + nums[k] == target`，统计三个数的奇数个数；若 ≥ 2 则加入答案，然后同时移动 `j`、`k` 并跳过重复值。\n   - 若和小于 `target`，`j` 右移。\n   - 若和大于 `target`，`k` 左移。\n4. 由于已排序，答案中的每个三元组自然升序，组间字典序也自然满足。\n\n**去重关键**：固定第一个元素和双指针移动时，均需跳过连续相同值，避免产生重复三元组。\n\n## 复杂度分析\n\n- **时间复杂度**：$O(n^2)$。排序 $O(n \\log n)$，枚举 `i` 和双指针总计 $O(n^2)$。\n- **空间复杂度**：$O(1)$（不计输出数组）。排序使用原地排序，双指针不需要额外空间。\n\n## 代码实现\n\n#code-switcher\n```python\nclass Solution:\n    def threeSumWithParity(self, nums: list[int], target: int) -> list[list[int]]:\n        # 排序便于双指针和去重\n        nums.sort()\n        n = len(nums)\n        ans = []\n        for i in range(n - 2):\n            # 跳过重复的第一个元素，避免重复三元组\n            if i > 0 and nums[i] == nums[i - 1]:\n                continue\n            j, k = i + 1, n - 1\n            while j < k:\n                s = nums[i] + nums[j] + nums[k]\n                if s == target:\n                    # 统计三个数中的奇数个数\n                    odd = (nums[i] & 1) + (nums[j] & 1) + (nums[k] & 1)\n                    if odd >= 2:\n                        ans.append([nums[i], nums[j], nums[k]])\n                    # 跳过重复的 j 和 k\n                    while j < k and nums[j] == nums[j + 1]:\n                        j += 1\n                    while j < k and nums[k] == nums[k - 1]:\n                        k -= 1\n                    j += 1\n                    k -= 1\n                elif s < target:\n                    j += 1\n                else:\n                    k -= 1\n        return ans\n```\n\n```java\nimport java.util.*;\n\nclass Solution {\n    public List<List<Integer>> threeSumWithParity(List<Integer> nums, int target) {\n        // 排序便于双指针和去重\n        Collections.sort(nums);\n        int n = nums.size();\n        List<List<Integer>> ans = new ArrayList<>();\n        for (int i = 0; i < n - 2; i++) {\n            // 跳过重复的第一个元素，避免重复三元组\n            if (i > 0 && nums.get(i).equals(nums.get(i - 1))) continue;\n            int j = i + 1, k = n - 1;\n            while (j < k) {\n                long s = (long)nums.get(i) + nums.get(j) + nums.get(k);\n                if (s == target) {\n                    // 统计三个数中的奇数个数\n                    int odd = (nums.get(i) & 1) + (nums.get(j) & 1) + (nums.get(k) & 1);\n                    if (odd >= 2) {\n                        ans.add(Arrays.asList(nums.get(i), nums.get(j), nums.get(k)));\n                    }\n                    // 跳过重复的 j 和 k\n                    while (j < k && nums.get(j).equals(nums.get(j + 1))) j++;\n                    while (j < k && nums.get(k).equals(nums.get(k - 1))) k--;\n                    j++; k--;\n                } else if (s < target) {\n                    j++;\n                } else {\n                    k--;\n                }\n            }\n        }\n        return ans;\n    }\n}\n```\n\n```cpp\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> threeSumWithParity(vector<int>& nums, int target) {\n        // 排序便于双指针和去重\n        sort(nums.begin(), nums.end());\n        int n = nums.size();\n        vector<vector<int>> ans;\n        for (int i = 0; i < n - 2; ++i) {\n            // 跳过重复的第一个元素，避免重复三元组\n            if (i > 0 && nums[i] == nums[i - 1]) continue;\n            int j = i + 1, k = n - 1;\n            while (j < k) {\n                // 使用 long long 防止 int 溢出\n                long long s = (long long)nums[i] + nums[j] + nums[k];\n                if (s == target) {\n                    // 统计三个数中的奇数个数\n                    int odd = (nums[i] & 1) + (nums[j] & 1) + (nums[k] & 1);\n                    if (odd >= 2) {\n                        ans.push_back({nums[i], nums[j], nums[k]});\n                    }\n                    // 跳过重复的 j 和 k\n                    while (j < k && nums[j] == nums[j + 1]) ++j;\n                    while (j < k && nums[k] == nums[k - 1]) --k;\n                    ++j; --k;\n                } else if (s < target) {\n                    ++j;\n                } else {\n                    --k;\n                }\n            }\n        }\n        return ans;\n    }\n};\n```\n\n```javascript\nvar threeSumWithParity = function(nums, target) {\n    // 排序，便于双指针和去重\n    nums.sort(function(a, b) { return a - b; });\n    var n = nums.length;\n    var ans = [];\n    for (var i = 0; i < n - 2; i++) {\n        // 跳过重复的第一个元素\n        if (i > 0 && nums[i] === nums[i - 1]) continue;\n        var j = i + 1, k = n - 1;\n        while (j < k) {\n            var s = nums[i] + nums[j] + nums[k];\n            if (s === target) {\n                // 统计三个数中的奇数个数\n                var odd = (nums[i] & 1) + (nums[j] & 1) + (nums[k] & 1);\n                if (odd >= 2) {\n                    ans.push([nums[i], nums[j], nums[k]]);\n                }\n                // 跳过重复的 j 和 k\n                while (j < k && nums[j] === nums[j + 1]) j++;\n                while (j < k && nums[k] === nums[k - 1]) k--;\n                j++;\n                k--;\n            } else if (s < target) {\n                j++;\n            } else {\n                k--;\n            }\n        }\n    }\n    return ans;\n};\n```\n\n```c\n#include <stdlib.h>\n\n// 升序排序比较函数\nstatic int cmp(const void* a, const void* b) {\n    int x = *(int*)a, y = *(int*)b;\n    if (x < y) return -1;\n    if (x > y) return 1;\n    return 0;\n}\n\nint** threeSumWithParity(int* nums, int numsSize, int target, int* returnSize, int** returnColumnSizes) {\n    // 排序，便于双指针和去重\n    qsort(nums, numsSize, sizeof(int), cmp);\n\n    int cap = 4096;\n    int** ans = (int**)malloc(cap * sizeof(int*));\n    *returnColumnSizes = (int*)malloc(cap * sizeof(int));\n    int cnt = 0;\n\n    for (int i = 0; i < numsSize - 2; i++) {\n        // 跳过重复的第一个元素\n        if (i > 0 && nums[i] == nums[i - 1]) continue;\n        int j = i + 1, k = numsSize - 1;\n        while (j < k) {\n            long long s = (long long)nums[i] + nums[j] + nums[k];\n            if (s == target) {\n                // 统计奇数个数\n                int odd = (nums[i] & 1) + (nums[j] & 1) + (nums[k] & 1);\n                if (odd >= 2) {\n                    if (cnt >= cap) {\n                        cap *= 2;\n                        ans = (int**)realloc(ans, cap * sizeof(int*));\n                        *returnColumnSizes = (int*)realloc(*returnColumnSizes, cap * sizeof(int));\n                    }\n                    ans[cnt] = (int*)malloc(3 * sizeof(int));\n                    ans[cnt][0] = nums[i];\n                    ans[cnt][1] = nums[j];\n                    ans[cnt][2] = nums[k];\n                    (*returnColumnSizes)[cnt] = 3;\n                    cnt++;\n                }\n                // 跳过重复的 j 和 k\n                while (j < k && nums[j] == nums[j + 1]) j++;\n                while (j < k && nums[k] == nums[k - 1]) k--;\n                j++;\n                k--;\n            } else if (s < target) {\n                j++;\n            } else {\n                k--;\n            }\n        }\n    }\n\n    *returnSize = cnt;\n    return ans;\n}\n```\n\n#code-switcher\n#hot100-card {\n  title: \"LeetCode Hot 100系统复习笔记\",\n  btn: \"开始复习\",\n  href: \"/codenote/hot100\",\n}\n"}