{"title": "查找温度记录统计信息(100分)", "topics": ["暴力枚举"], "pid": "P5108", "difficulty": 4, "date": "2026年6月10日", "description_md": "# 题目内容\n\n新能源电站配备了多个温度传感器，用于监测关键设备的运行温度，需要定期分析温度数据：找出历史最高温度记录，并检测是否存在温度异常升高的危险时段。\n\n\"危险时段\" 定义为一段连续$ k $个时间点的温度**严格单调递增**，且该时段内温度升高总幅度 ≥ 阈值$ t$，这样的时段被标记为设备存在过热风险。\n\n给定一个整数数组 `temperatures`，其中 `temperatures[i]` 表示第$ i $个时间点的温度记录，请完成以下任务：\n\n1. 找出最高温度值及其首次出现的下标位置\n2. 统计所有 \"危险时段\" 的数量\n3. 找出温度升高幅度最大的 \"危险时段\" 的起始和结束位置\n\n\n\n## 输入\n\n- `temperatures: int[]`，温度记录序列，`1 ≤ 长度 ≤ 10000`，`-100 ≤ temperatures[i] ≤ 100`\n\n- `k: int`，危险时段所需的连续点数，`2 ≤ k ≤ temperatures.length`\n\n- `t: int`，连续升高的温度差值阈值，`0 < t ≤ 100`\n\n  \n\n## 输出\n\nint[]，长度为 5 的数组：[最高温度值, 最高温度首次出现下标, 危险时段数量, 最大危险时段起始位置, 最大危险时段结束位置]\n\n## 补充说明\n\n- 严格单调递增：`temperatures[i] < temperatures[i+1] < ... < temperatures[i+k-1]`\n- 若无危险时段，起始和结束位置均返回 `-1`\n- 若多个危险时段升高幅度相同且最大，取起始位置最小的那个\n- 下标从 $0$ 开始计数\n\n## 样例1 \n\n**输入** \n\n```\n8,[20,25,30,28,35,40,42,25],3,10\n```\n\n**输出** \n\n```\n[42,6,2,3,5]\n```\n\n**说明**\n\n最高温度 $42$ 度，出现在位置 $6$\n\n危险时段检测（$k=3, t=10$）：\n\n- 位置 $0-2$：$20→25→30$，升高 $10 $度 ($≥10$) ✓ 危险时段$ 1$\n- 位置$3-5$：$28→35→40$，升高 $12$ 度 ($≥10$) ✓ 危险时段 $2$\n- 位置 $4-6$：$35→40→42$，升高 $7$ 度 ($<10$) ✗ 不是危险时段\n- 其他时段不满足条件\n\n危险时段数量：$2$ 个\n\n升高幅度最大：位置 $3-5$（$12$ 度）\n\n\n\n## 样例2\n\n**输入** \n\n```\n3,[10,15,20],3,30\n```\n\n**输出** \n\n```\n[20,2,0,-1,-1]\n```\n\n**说明**\n\n- 位置 $0-2$：$10→15→20 (<30)$ 非危险时段\n- 最高温度是$ 20$，位置下标为 $2$\n- 危险时段 $0 $个\n- 危险时段起始位置为 $- 1$，结束位置为 $-1$\n\n## 样例3\n\n**输入** \n\n```\n6,[1, 3, 6, 2, 4, 7],3,5\n```\n\n**输出** \n\n```\n[7,5,2,0,2]\n```\n\n**说明**\n\n最高温度值：$7$\n\n最高温度首次出现下标：$5$\n\n危险时段检测（$k=3, t=5$）：\n\n- 位置$ 0-2$：$1→3→6$，升高$ 5$ 度$ (≥5) $✓ 危险时段 $1$\n- 位置 $3-5$：$2→4→7$，升高 $5$ 度 $(≥5) $✓ 危险时段 $2$\n\n危险时段数量：$2$\n\n最大危险时段起始位置：$0$（$2$ 个相同最大温差，取第 $1$ 个危险时段起始下标返回）\n\n最大危险时段结束位置：$2$\n\n\n\n", "has_signature": false, "python_signature": null, "accept_rate": "58/224", "code_templates": {"py": "# 空白解题模板\n\nclass Solution:\n    def solve(self):\n        # Write your code here\n        pass\n", "java": "// 空白解题模板\n\nclass Solution {\n    public void solve() {\n        // Write your code here\n        \n    }\n}\n", "cc": "// 空白解题模板\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    void solve() {\n        // Write your code here\n        \n    }\n};\n", "js": "// JavaScript空白模板\n\n// Write your code here\n", "c": "// C语言空白模板\n\n#include <stdio.h>\n#include <stdlib.h>\n\n// Write your code here\n"}, "solution": "## 解题思路\n\n本题是**数组扫描 + 滑动窗口判定**，可按以下步骤处理：\n\n1. **最高温度**：线性扫描 `temperatures`，维护当前最大值及其**首次出现**下标（仅在严格变大时更新下标）。\n2. **危险时段枚举**：对每个起点 $i \\in [0,\\ n-k]$，检查长度 $k$ 的窗口是否**严格单调递增**：\n   - 即对所有 $j \\in [i,\\ i+k-2]$，需满足 $\\texttt{temperatures}[j] < \\texttt{temperatures}[j+1]$；\n   - 相邻相等（如 $2,2,3$）不满足「严格递增」，不能计入。\n3. **阈值过滤**：窗口合法时，计算升幅 $\\texttt{temperatures}[i+k-1]-\\texttt{temperatures}[i]$，仅当 $\\ge t$ 时计为一个危险时段。\n4. **最优窗口**：在所有危险时段中，取升幅最大者；若升幅相同，取**起始下标最小**的窗口，输出其起止下标（含端点）；若无危险时段，后两项返回 $-1$。\n\n**常见假解**：\n\n- 只判断首尾差值、不检查中间是否严格递增 → $[1,2,2,5]$ 在 $k=4$ 时首尾差够大但中间有相等元素；\n- 把「非递减」当成危险时段 → 相邻相等应排除；\n- 最高温取下标最后一次出现 → 题目要求**首次**出现位置；\n- 升幅相同时取结束下标最小或最后一个窗口 → 应取起始下标最小；\n- 用 $>$ 而非 $\\ge$ 判断阈值 → $t=10$ 时升幅恰好 $10$ 也应计入；\n- 窗口长度弄错 → 危险时段固定为连续 $k$ 个点，不是任意更长上升段。\n\n## 复杂度分析\n\n设 $n = |\\texttt{temperatures}| \\le 10000$，$k \\le n$。\n\n- 时间：$O(nk)$。最多 $n-k+1$ 个起点，每个窗口检查 $k-1$ 对相邻元素；在题面约束下足够；\n- 空间：$O(1)$ 额外空间（不计输入数组）。\n\n## 代码实现\n\n#code-switcher\n```python\nfrom typing import List\n\n\nclass Solution:\n    def analyzeTemperatureData(\n        self, temperatures: List[int], k: int, t: int\n    ) -> List[int]:\n        n = len(temperatures)\n        max_val = temperatures[0]\n        max_idx = 0\n        # 线性扫描：仅在严格变大时更新下标，保证是首次出现\n        for i in range(1, n):\n            if temperatures[i] > max_val:\n                max_val = temperatures[i]\n                max_idx = i\n\n        count = 0\n        best_start = -1\n        best_end = -1\n        best_rise = -1\n\n        for i in range(0, n - k + 1):\n            ok = True\n            # 检查长度为 k 的窗口是否严格单调递增\n            for j in range(i, i + k - 1):\n                if temperatures[j] >= temperatures[j + 1]:\n                    ok = False\n                    break\n            if not ok:\n                continue\n            rise = temperatures[i + k - 1] - temperatures[i]\n            if rise >= t:\n                count += 1\n                # 升幅更大优先；升幅相同时取起始下标更小\n                if rise > best_rise or (\n                    rise == best_rise and (best_start == -1 or i < best_start)\n                ):\n                    best_rise = rise\n                    best_start = i\n                    best_end = i + k - 1\n\n        return [max_val, max_idx, count, best_start, best_end]\n```\n\n```java\npublic class Solution {\n    public int[] analyzeTemperatureData(int[] temperatures, int k, int t) {\n        int n = temperatures.length;\n        int maxVal = temperatures[0];\n        int maxIdx = 0;\n        // 首次出现：只有严格大于才刷新下标\n        for (int i = 1; i < n; i++) {\n            if (temperatures[i] > maxVal) {\n                maxVal = temperatures[i];\n                maxIdx = i;\n            }\n        }\n\n        int count = 0;\n        int bestStart = -1;\n        int bestEnd = -1;\n        int bestRise = -1;\n\n        for (int i = 0; i <= n - k; i++) {\n            boolean ok = true;\n            // 相邻相等则不是严格递增\n            for (int j = i; j < i + k - 1; j++) {\n                if (temperatures[j] >= temperatures[j + 1]) {\n                    ok = false;\n                    break;\n                }\n            }\n            if (!ok) continue;\n            int rise = temperatures[i + k - 1] - temperatures[i];\n            if (rise >= t) {\n                count++;\n                if (rise > bestRise\n                        || (rise == bestRise && (bestStart == -1 || i < bestStart))) {\n                    bestRise = rise;\n                    bestStart = i;\n                    bestEnd = i + k - 1;\n                }\n            }\n        }\n\n        return new int[] {maxVal, maxIdx, count, bestStart, bestEnd};\n    }\n}\n```\n\n```cpp\n#include <vector>\n\nusing namespace std;\n\nclass Solution {\n public:\n  vector<int> analyzeTemperatureData(vector<int>& temperatures, int k, int t) {\n    int n = (int)temperatures.size();\n    int max_val = temperatures[0];\n    int max_idx = 0;\n    // 维护历史最高温及其首次下标\n    for (int i = 1; i < n; i++) {\n      if (temperatures[i] > max_val) {\n        max_val = temperatures[i];\n        max_idx = i;\n      }\n    }\n\n    int count = 0;\n    int best_start = -1;\n    int best_end = -1;\n    int best_rise = -1;\n\n    for (int i = 0; i <= n - k; i++) {\n      bool ok = true;\n      // 窗口内每一对相邻元素都要严格变大\n      for (int j = i; j < i + k - 1; j++) {\n        if (temperatures[j] >= temperatures[j + 1]) {\n          ok = false;\n          break;\n        }\n      }\n      if (!ok) continue;\n      int rise = temperatures[i + k - 1] - temperatures[i];\n      if (rise >= t) {\n        count++;\n        if (rise > best_rise ||\n            (rise == best_rise && (best_start == -1 || i < best_start))) {\n          best_rise = rise;\n          best_start = i;\n          best_end = i + k - 1;\n        }\n      }\n    }\n\n    return {max_val, max_idx, count, best_start, best_end};\n  }\n};\n```\n\n```javascript\n/**\n * @param {number[]} temperatures - 温度记录序列\n * @param {number} k - 危险时段的连续点数\n * @param {number} t - 温度升高阈值\n * @return {number[]} - [最高温度值, 最高温度首次出现下标, 危险时段数量, 最大危险时段起始位置, 最大危险时段结束位置]\n */\nvar analyzeTemperatureData = function(temperatures, k, t) {\n    var n = temperatures.length;\n\n    // 1. 最高温度及其首次出现下标\n    var maxVal = temperatures[0];\n    var maxIdx = 0;\n    for (var i = 1; i < n; i++) {\n        if (temperatures[i] > maxVal) {\n            maxVal = temperatures[i];\n            maxIdx = i;\n        }\n    }\n\n    // 2. 危险时段检测\n    var count = 0;\n    var bestStart = -1;\n    var bestEnd = -1;\n    var bestRise = -1;\n\n    for (var i = 0; i <= n - k; i++) {\n        // 检查长度为 k 的窗口是否严格单调递增\n        var ok = true;\n        for (var j = i; j < i + k - 1; j++) {\n            if (temperatures[j] >= temperatures[j + 1]) {\n                ok = false;\n                break;\n            }\n        }\n        if (!ok) continue;\n\n        var rise = temperatures[i + k - 1] - temperatures[i];\n        if (rise >= t) {\n            count++;\n            // 升幅更大优先；升幅相同时取起始下标更小\n            if (rise > bestRise ||\n                (rise === bestRise && (bestStart === -1 || i < bestStart))) {\n                bestRise = rise;\n                bestStart = i;\n                bestEnd = i + k - 1;\n            }\n        }\n    }\n\n    return [maxVal, maxIdx, count, bestStart, bestEnd];\n};\n```\n\n```c\n#include <stdlib.h>\n#include <string.h>\n\n/**\n * @param temperatures 温度记录数组\n * @param temperaturesSize 温度记录数组长度\n * @param k 连续时间点数量\n * @param t 温度升高阈值\n * @param returnSize 返回数组长度指针\n * @return 长度为5的结果数组\n */\nint* analyzeTemperatureData(int* temperatures, int temperaturesSize, int k, int t, int* returnSize) {\n    *returnSize = 5;\n    int* result = (int*)malloc(5 * sizeof(int));\n\n    int n = temperaturesSize;\n\n    // 1. 最高温度及其首次出现下标\n    int maxVal = temperatures[0];\n    int maxIdx = 0;\n    for (int i = 1; i < n; i++) {\n        if (temperatures[i] > maxVal) {\n            maxVal = temperatures[i];\n            maxIdx = i;\n        }\n    }\n\n    // 2. 危险时段检测\n    int count = 0;\n    int bestStart = -1;\n    int bestEnd = -1;\n    int bestRise = -1;\n\n    for (int i = 0; i <= n - k; i++) {\n        // 检查长度为 k 的窗口是否严格单调递增\n        int ok = 1;\n        for (int j = i; j < i + k - 1; j++) {\n            if (temperatures[j] >= temperatures[j + 1]) {\n                ok = 0;\n                break;\n            }\n        }\n        if (!ok) continue;\n\n        int rise = temperatures[i + k - 1] - temperatures[i];\n        if (rise >= t) {\n            count++;\n            // 升幅更大优先；升幅相同时取起始下标更小\n            if (rise > bestRise ||\n                (rise == bestRise && (bestStart == -1 || i < bestStart))) {\n                bestRise = rise;\n                bestStart = i;\n                bestEnd = i + k - 1;\n            }\n        }\n    }\n\n    result[0] = maxVal;\n    result[1] = maxIdx;\n    result[2] = count;\n    result[3] = bestStart;\n    result[4] = bestEnd;\n\n    return result;\n}\n```\n#code-switcher\n\n#hot100-card {\n  title: \"LeetCode Hot 100系统复习笔记\",\n  btn: \"开始复习\",\n  href: \"/codenote/hot100\",\n}\n"}